import React from 'react';

const ListingSlider = ({ listing }) => {
  console.log(listing);

  return (
    <React.Fragment>
      <h1>Hello World</h1>
    </React.Fragment>
  );
};

export default ListingSlider;
